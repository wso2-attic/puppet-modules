---
:hierarchy:
    - "node/%{::clientcert}"
    - "deployment/%{::product_name}/%{::product_version}/%{::product_profile}"
    - "deployment/%{::product_name}/%{::product_version}/default"
    - "osfamily/%{::osfamily}"
    - "vm_type/%{::vm_type}"
    - common
:backends:
    - yaml
:yaml:
    :datadir: "hiera/hieradata/%{::environment}"
